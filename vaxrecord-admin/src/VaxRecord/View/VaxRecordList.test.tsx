import VaxRecordList from "./VaxRecordList";
import {render} from "@testing-library/react";

describe('VaxRecordList View', () => {

    // it('display This is List', async () => {
    //     const renderedComponent = render(<VaxRecordList/>);
    //
    //     const renderedComponentBody = renderedComponent.getByText('This is List');
    //     expect(renderedComponentBody).toBeInTheDocument();
    // })
})